services:
  - type: web
    name: ariah-trading-bot
    runtime: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python main.py"
    envVars:
      - key: TELEGRAM_TOKEN
        fromEnv: TELEGRAM_TOKEN
      - key: CHAT_ID
        fromEnv: CHAT_ID
